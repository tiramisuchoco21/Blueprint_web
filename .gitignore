# ── 개인정보: 커밋 금지 ──────────────────────────────────
# raw_card.txt 는 실제 카드 명세다. 의료 이용 기록·부분 마스킹된 번호·
# 생활 반경이 포함돼 있어 공개 저장소에 올리면 안 된다.
# 저장소에 포함된 것은 fixtures/demo_card.txt (합성 데이터)뿐이다.
fixtures/raw_card.txt
fixtures/카드*.txt
fixtures/_*.txt

# ── 비밀 ─────────────────────────────────────────────────
.env
*.key
*.pem

# ── 파이썬 ───────────────────────────────────────────────
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/
.DS_Store

# ── 공모전 기획 원본 (공개 저장소에 올리지 않음) ───────────
# 필요하면 아래 줄을 지우고  git add -f *.pdf  로 포함할 수 있다.
*.pdf
